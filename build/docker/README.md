Contains definitions for the docker images, including the web app and nginx.

To prepare for building the Docker images run.

    npm run prep:release

Then cd to `bin/docker` and run

   docker-compose up
