import * as React from 'react';
import { configPage } from 'shared/';
import { EPG as template } from 'shared/page/pageTemplate';

const EPG = (props: PageProps) => <div>EPG</div>;

export default configPage(EPG, { template });
