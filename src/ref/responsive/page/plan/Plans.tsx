import * as React from 'react';
import { configPage } from 'shared/';
import { STATIC as template } from 'shared/page/pageTemplate';
import { PlanSelection as key } from 'shared/page/pageKey';

const Plans = (props: PageProps) => <div>Plans</div>;

export default configPage(Plans, { template, key });
